# Solar pipeline Python requirements.
# Mostly overlaps with requirements-wind.txt; pvlib is the new dependency.

# Core scientific stack
numpy>=1.24
pandas>=2.0,<4
scipy>=1.18.0
pytz  # gridstatus pins this; pvlib uses it for timezone handling

# Geospatial / GRIB
xarray>=2023.0
cfgrib>=0.9.10
eccodes>=1.6
herbie-data>=2024.0.0,<2027

# Solar physics — sun position, tracker geometry, POA irradiance, PVWatts
pvlib>=0.10,<1

# Web/data
requests>=2.31

# Excel reading (for EIA-860M monthly data supplement)
openpyxl>=3.1

# Output
pyarrow>=14.0
plotly>=5.18
