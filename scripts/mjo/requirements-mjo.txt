numpy
scipy
pandas
xarray
dask
netcdf4
h5netcdf
zarr             # ARCO-ERA5 is a zarr store: aam.py's observed_history() and the
gcsfs            # build_*_clim.py builders read it. Without these the ERA5 tail
                 # SILENTLY vanishes - observed_history catches the ImportError
                 # and returns None, so the figure still builds, just without its
                 # observed history (seen on the 2026-08-29 Actions trial).
cfgrib
eccodes
matplotlib==3.11.2  # 3.11 breaks cartopy GeoAxes tight-bbox/titles (2026-08-25)
eofs
requests
ecmwf-opendata
cartopy==0.25.0  # 0.26.0 collapses set_extent((-180, 180, ...)) on a dateline-centred PlateCarree to a 0.2-deg strip (blank global SST panel, 2026-09-18); pinned to the laptop's version
shapely>=2.1.2
pyproj>=3.7.2
pyshtools>=4.13   # velocity-potential spherical-harmonic inversion (wind200_vpot.py); prebuilt wheels
