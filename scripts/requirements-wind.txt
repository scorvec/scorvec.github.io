# Core scientific stack
numpy>=2.5.2
pandas>=2.0,<4
scipy>=1.11
pytz  # gridstatus 0.34 imports pytz; pandas 3.x doesn't pull it in

# Geospatial / GRIB
xarray>=2023.0
cfgrib>=0.9.15.1
eccodes>=2.48.0
herbie-data>=2026.3.0,<2027

# Power-curve catalog
windpowerlib>=0.2.2

# Output
pyarrow>=14.0
openpyxl>=3.1
plotly>=5.18

# Verification: actuals from ISOs
# - gridstatus is the open-source library that scrapes ISO public sites
#   directly (no API key, no rate limit, NOT the gridstatus.io hosted
#   API — that dependency was removed 2026-07). Used for SPP and CAISO
#   actuals. Pinned to 0.34 since newer versions have tighter
#   cryptography pins that conflict with other deps and cause pip
#   backtracking.
gridstatus==0.34.0
